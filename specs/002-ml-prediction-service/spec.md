<style>
.speckit-viewer-inline, .speckit-viewer-inline h1, .speckit-viewer-inline h2, .speckit-viewer-inline h3, .speckit-viewer-inline h4, .speckit-viewer-inline p, .speckit-viewer-inline li, .speckit-viewer-inline td, .speckit-viewer-inline th, .speckit-viewer-inline strong, .speckit-viewer-inline em, .speckit-viewer-inline blockquote { color: #111111 !important; font-style: normal; }
.speckit-viewer-inline { background: #ffffff !important; }
.speckit-viewer-inline pre, .speckit-viewer-inline pre * { color: #e2e8f0 !important; }
</style>

# 무엇을 만들었나 (spec)

**LoL 경기 10분 시점의 상황을 넣으면, ① 이길 확률 ② 왜 유리한지 ③ 믿어도 되는지를 돌려주는 서비스.**

2026-09-01 구현 완료. 이 문서는 팀원 누구나 읽을 수 있게 간결하게 정리한 판이며,
계획 단계의 상세 원본은 저장소 `specs/002-ml-prediction-service/_server_backup_2026-09-01/` 에 있다.

## 누가 쓰나

| 사용자 | 원하는 것 |
|---|---|
| 관전하는 사람 | 지금 어느 팀이 왜 유리한지 |
| 복기하는 유저 | 진 경기에서 뭐부터 고칠지 |
| 팀원·후속 개발자 | 그대로 다시 실행해서 같은 결과를 얻는 것 |

## 만든 기능

| 기능 | 내용 | 상태 |
|---|---|---|
| 승률 예측 | 13개 숫자를 넣으면 "파란 팀이 이길 확률 73%" | ✅ |
| 이유 설명 | 이 판을 움직인 요인 순위 (돈 차이 1위, 경험치 2위...) | ✅ |
| 경고 | 팽팽한 경기라 잘 틀림 / 학습 범위 밖 입력임 | ✅ |
| 웹 화면 | 입력 폼 → 승률 막대 + 이유 + 경고 한 화면 | ✅ |
| 시점 바꾸기 | 10분 대신 15분 데이터로도 같은 코드로 재학습 가능 | ✅ |
| 10분 vs 15분 비교 | 5분을 더 보면 얼마나 좋아지나 (프로 경기로 실험) | ✅ |
| 경기 유형 분류 | 운영전·난타전·일방적·시야전 4가지 | ✅ |
| 내 경기 복기 | 라이엇에서 끝난 경기를 불러와 10분 시점 승률 계산 | ✅ (덤) |

## 지킨 규칙

- **예측은 전부 학습된 모델이 한다.** "돈 차이 크면 승리" 같은 규칙을 사람이 써넣지 않는다.
  넣는 순간 "데이터가 검증했다"는 말이 거짓이 되기 때문이다.
- **시험지 봉인.** 데이터를 연습용 7,903판 / 시험용 1,976판으로 먼저 나누고,
  시험용은 마지막 채점 때 딱 한 번만 열었다. 미리 보면 점수가 가짜로 오른다.
- **화면은 보여주기만.** 웹은 예측 파일(predict.py)을 그대로 불러 쓴다.
  계산이 한 곳에만 있어서 화면 숫자와 모델 숫자가 어긋날 수 없고, 자동 대조 테스트도 있다.
- **저장한 모델은 복원 검증.** 파일로 저장한 모델을 다시 불러왔을 때
  똑같은 답이 나오는지 자동으로 확인한다.

## 채점표 (성공 기준)

| 기준 | 목표 | 결과 |
|---|---|---|
| 정확도 | 0.70 이상 | ✅ **0.7394** (찍기 0.50 대비 +24점) |
| 점수가 흔들리지 않을 것 | 편차 0.02 미만 | ✅ 0.0153 |
| 외우지 않았을 것 | 연습-검증 격차 0.03 미만 | ✅ +0.0022 |
| 이유가 방법을 바꿔도 같을 것 | 두 방법 일치 | ✅ 돈 > 경험치 > 드래곤, 완전 일치 |
| 언제 틀리는지 말할 수 있을 것 | 수치로 제시 | ✅ 팽팽 0.615 ↔ 결정 0.947 |
| 제출물 6종 | 전부 검증 | ✅ 6/6 |
| 게임 모르는 사람 테스트 | 1~2명 | ⬜ 아직 |

## 하지 않는 것

진행 중 경기의 실시간 연동(라이엇이 데이터를 안 줌) · 개인 실력 평가 · 베팅 등 상업적 이용

# 무엇을 만들었나 (spec)

**LoL 경기 10분 시점의 상황을 넣으면 ① 이길 확률 ② 왜 유리한지 ③ 믿어도 되는지를 돌려주는 예측·설명 파이프라인.**

> 발표 주제 — *LoL 경기는 언제 승부가 결정되는가? 인게임 시점별 경기 데이터 기반 승패 예측 및 핵심 승리요인 분석 (4조)*

2026-09-01 구현·배포 완료. 팀원 누구나 읽을 수 있게 정리한 판이다.
계획 단계의 긴 원본은 `_server_backup_2026-09-01/`, 실측 근거는 `docs/experiment_report.md`·`docs/data_analysis.md`에 있다.
**공개 주소: https://p4.sumzip.com** (프런트 9504 · 백엔드 9524, `./check_project.sh`).

## 왜 만들었나 — 캔버스의 미션 3개와 결과

| 캔버스 [미션] | 우리가 실제로 한 것 |
|---|---|
| 관전의 "왜"를 채운다 | 승률 숫자에 요인 순위를 붙였다 — **돈 차이 > 경험치 차이 > 드래곤** |
| 분석의 접근성 격차를 좁힌다 | 공개 데이터 + 기본 ML만으로 복기 우선순위를 뽑는 절차를 스크립트 6개로 남겼다 |
| 통념을 검증한다 | 10분 vs 15분을 통제 비교했다 — "초반에 다 기운다"도 "끝까지 모른다"도 틀렸고, **경기 성격에 따라 다르다** |

## 누가 쓰나

| 사용자 | 원하는 것 |
|---|---|
| 관전하는 사람 | 지금 어느 팀이 왜 유리한지 |
| 복기하는 유저 | 진 경기에서 뭐부터 고칠지 |
| 팀원·후속 개발자 | 그대로 다시 실행해서 같은 결과를 얻는 것 |

## 만든 기능 (FR-1 ~ FR-9)

| 번호 | 기능 | 내용 | 결과 |
|---|---|---|---|
| FR-1 | 승률 예측 | 13개 숫자를 넣으면 "파란 팀이 이길 확률 73%" + 승/패 예측 | ✅ 데모 3건 51.2% / 94.6% / 16.8% |
| FR-2 | 이유 설명 | 요인 순위를 방향(+/−)과 크기로. 계수·permutation 두 방법으로 교차 확인 | ✅ 상위 3개 순위 완전 일치 |
| FR-3 | 관전 포인트 | 요인 순위를 "게임 몰라도 볼 수 있는 지표 목록"으로 | ✅ README 부록 |
| FR-4 | 언제 틀리나 | 골드차 구간별 정확도로 "접전일수록 틀린다" 검증 | ✅ 팽팽 0.615 ↔ 결정 0.947 |
| FR-5 | 경기 유형 | 진영 정보를 지운 KMeans 4유형 + 유형별 10분 리드팀 승률 | ✅ 운영전·난타전·일방적·시야전 |
| FR-6 | 시점 바꾸기 | `TIME_POINT` 인자만 바꿔 15분 데이터로 같은 코드 재학습 | ✅ |
| FR-7 | 10분 vs 15분 비교 | 같은 경기·같은 피처·같은 분할에서 시점만 바꿈 (프로 경기) | ✅ +5.25%p, 팽팽한 경기만 +7.8%p |
| FR-8 | 찍기 대비 개선 | 기준선 0.50 대비 개선 폭 병기 | ✅ +23.8%p |
| FR-9 | 웹 서비스 | 입력 폼 → 승률 막대 + 이유 + 경고 + 서버 리포트. 백엔드 API 8개 | ✅ https://p4.sumzip.com, 화면 = 모델 숫자 6건 일치 |
| 덤 | 내 경기 복기 | 라이엇 API로 끝난 경기를 불러와 10분 시점 승률 계산 | ✅ 키는 환경변수로만 |

## 지킨 규칙

- **예측은 전부 학습된 모델이 한다.** "돈 차이 크면 승리" 같은 규칙을 사람이 써넣지 않는다.
  넣는 순간 "데이터가 검증했다"는 말이 거짓이 되기 때문이다. 확률 수동 보정·외부 승률 API도 금지.
- **시험지 봉인.** 연습용 7,903판 / 시험용 1,976판으로 **어떤 전처리보다 먼저** 나누고,
  시험용은 마지막 채점 때 딱 한 번만 열었다. 미리 보면 점수가 가짜로 오른다.
- **화면은 보여주기만.** 백엔드는 `predict.py`를 그대로 import하고, 프런트는 `/api`를 중계만 한다.
  계산이 한 곳에만 있어서 화면 숫자와 모델 숫자가 어긋날 수 없고, 자동 대조 테스트(`web/test_parity.py`)도 있다.
- **저장한 모델은 복원 검증.** 파일로 저장한 모델을 다시 불러왔을 때 똑같은 답이 나오는지 자동 확인한다.
- **두 실험의 절대 수치를 섞지 않는다.** 실험 A(다이아 랭크)와 B(프로 경기)는 데이터·실력대·피처 수가 달라서 서로 비교하지 않는다.

## 채점표 (SC-1 ~ SC-7)

| 번호 | 기준 | 목표 | 결과 |
|---|---|---|---|
| SC-1 | 정확도 + 찍기 대비 개선 폭 | 0.70 이상 | ✅ **0.7394** (+23.8%p) · 10번 반복 0.7366 ± 0.0081 |
| SC-2 | 점수가 흔들리지 않을 것 | 편차 < 0.02, 연습−검증 격차 < 0.03 | ✅ 0.0153 / +0.0022 |
| SC-3 | 이유가 방법을 바꿔도 같을 것 | 두 방법 상위 일치 | ✅ 돈 > 경험치 > 드래곤 |
| SC-4 | 언제 틀리는지 수치로 | 구간별 정확도 | ✅ 0.615 → 0.947 |
| SC-5 | 제출물 6종 + 복원 검증 | 전부 | ✅ 6/6 |
| SC-6 | 동일 집합 10분 vs 15분 비교표 | 있을 것 | ✅ |
| SC-7 | 게임 모르는 사람 테스트 | 1~2명 | ⬜ 아직 |

## 하지 않는 것 — 캔버스의 [부정적 영향]에 대한 답

**범위 밖**: 진행 중 경기의 실시간 연동(라이엇이 데이터를 안 줌) · 개인 실력 평가·픽밴 · 베팅 등 상업적 이용 · 입문자 효과 검증(후속 과제)

캔버스에서 걱정한 4가지는 `model_card.md`의 **사용 금지 상황 6가지**로 옮겼다:
① 베팅·도박 오용 ② 확률을 확정으로 과신 ③ 학습 범위 밖 입력에 그대로 적용
④ 경기 유형을 플레이스타일 폄하로 소비 ⑤ 다이아 결과를 다른 티어·프로에 일반화 ⑥ 패치가 바뀐 뒤 재학습 없이 사용
