<!-- ─────────────────────────────────────────────
  누구에게 무엇을 주는 서비스인가 · 기능을 어떤 순서로 넣을 것인가.
  왜 필요한가: op.gg 를 따라 만들면 op.gg 의 열등한 사본이 된다.
  우리 모델만 할 수 있는 것이 무엇인지 정해두고 거기서만 기능을 뽑기 위한 문서.
  주로 보는 사람: Claude(검수) · 서비스 작업자
  ───────────────────────────────────────────── -->

# 서비스 방향 — 우리 모델만 할 수 있는 것

## 0. 왜 예측하고 원인을 분석하나 — 이기려고

주제는 **승패 예측과 원인 분석**이지만, 사용자가 원하는 것은 예측도 분석도 아니다.
**이기는 것**이다. 그러니 우리 역할은 확률을 보여주는 게 아니라 **이기게 돕는 것**이다.

그래서 서비스는 세 단계를 끝까지 간다.

| | 단계 | 우리가 가진 것 | 없으면 |
|---|---|---|---|
| ① | **예측** — 지금 이기고 있나 | 10분 시점 승률 | 판단 기준이 없다 |
| ② | **원인** — 왜 그런가 | 지표별 기여도 · 4분면 판정 | "그래서 뭐" 로 끝난다 |
| ③ | **지시** — 그래서 뭘 하라 | **반사실 코칭** (`/api/coach`) | 진단서만 주고 끝내는 셈 |

②까지만 하면 "역전패 6판입니다" 로 끝난다. 사용자는 여전히 무엇을 바꿔야 할지 모른다.
**③이 있어야 감독이다.**

## 1. 근본 주제와 서비스의 연결

예측만 팔면 "확률 보여주는 사이트"이고, 그건 이미 많다.

**전적 사이트와 우리의 차이는 질문이 다르다는 것이다.**

| | 묻는 질문 | 답하는 방법 |
|---|---|---|
| 기존 전적 사이트 | **무슨 일이 있었나** | KDA · CS · 아이템 — 기록을 보여준다 |
| **우리** | **언제 갈렸나** | 10분 시점 모델로 그 판을 되감아 판정한다 |

10분 시점 모델이 있어야만 나오는 정보가 하나 있다 — **10분의 유불리와
최종 결과를 교차한 판정**이다.

| 10분 | 결과 | 판정 | 이 판에서 진단되는 것 |
|---|---|---|---|
| 유리 | 승 | **우세승** | 정상 |
| 불리 | 승 | **역전승** | 후반 집중력 |
| 불리 | 패 | **열세패** | **라인전 문제** |
| 유리 | 패 | **역전패** | **앞선 판을 못 끝냄 — 사용자가 먼저 다시 볼 판** |

같은 패배라도 처방이 정반대다. 열세패는 라인전을 고쳐야 하고,
역전패는 이긴 판을 닫는 법을 배워야 한다. **기존 전적 사이트는 이 둘을
구분해 주지 못한다** — 10분 시점의 예측이 없기 때문이다.
구현: `src/riot_api.py` 의 `verdict_of()`.

### 지시 — "그래서 뭘 하라" (`/api/coach`)

판정이 진단이라면 코칭은 처방이다. 모델을 거꾸로 돌려 계산한다.

> 현재 승률 43.0%
> · 드래곤 1마리 더 → **59.5%** (+16.5%p)
> · 경험치 600 더 → 54.8% (+11.8%p)
> · CS 20개 더 → 53.2% (+10.2%p)

**이게 가능한 이유가 곧 로지스틱 회귀를 채택한 이유다.** 확률이 '계수 x 표준화값'의
합이라 되짚을 수 있다. 랜덤포레스트를 골랐다면 이 기능 자체가 없다.
모델 선택의 근거(설명 가능성)가 서비스 기능으로 그대로 이어진다.

**함정 하나** — 계수를 그대로 쓰면 `KillsDiff`(−0.107)·`TotalMinionsKilledDiff`(−0.157)
때문에 **"킬하지 마라 · CS 먹지 마라"** 가 나온다. 골드를 고정한 채 CS 만 올리는 것은
현실에 없는 상황이라, 지표를 올릴 때 **따라오는 골드도 함께** 올린다.
`tests/test_coach.py` 가 모든 조언이 승률을 올리는지, 그리고 순진한 계산은
실제로 뒤집히는지까지 검사한다.

## 2. 타겟 — 누구에게

### 1차: 랭크를 올리려는 개인 유저

- **겪는 문제** — "왜 지는지 모르겠다." 전적 사이트는 KDA 를 보여주지만
  그게 원인인지 결과인지 말해 주지 않는다.
- **우리가 주는 것** — "최근 20판 중 **역전패 6판**. 라인전은 이기는데
  굴리질 못한다." 원인이 어느 쪽인지 가려 준다.
- **왜 우리여야 하나** — 판정에는 10분 시점 승률이 필요하고, 그건 모델이 있어야 나온다.

### 2차: 코치 · 스트리머

- 제자·시청자의 최근 판을 넣으면 **약점 유형이 한 줄로** 나온다.
- 방송에서 "이 사람은 초반형이다" 를 근거와 함께 말할 수 있다.

### 대상이 아닌 것

- **베팅·배당** — 하지 않는다. 분석 도구의 선을 넘지 않는다.
- **실시간 중계 오버레이** — 기술적으로 불가능하다 (4장).
- **아이템·룬 빌드 추천 (그 자체로는)** — 우리 데이터에 아이템이 없다. 모아서 승률만
  집계하면 다른 사이트와 같은 것이고, 우리 모델이 근거를 대지 못한다.
  **단, 판정과 교차하면 우리 것이 된다** — "당신이 **역전패**한 판들에서 공통적으로
  나타난 선택" 처럼. 이건 10분 판정이 있어야만 나온다. 5장의 챔피언 지표와 같은 원리다.

## 3. 기능 — 넣은 순서와 넣을 순서

원칙: **모든 기능은 "10분 시점 판정"에서 파생돼야 한다.**
그 뿌리에서 안 나오는 기능은 다른 사이트가 더 잘한다.

| | 기능 | 상태 | 뿌리와의 연결 |
|---|---|---|---|
| F1 | 실제 경기 복기 (시험셋 1,976판) | ✅ | 예측 vs 실제, 틀린 판도 공개 |
| F2 | 구간별 신뢰도 경고 | ✅ | 접전이면 "이 구간 적중률 61.5%" 를 먼저 |
| F3 | 승리요인 기여도 | ✅ | ② 원인 — 무엇이 그 판을 갈랐나 |
| F4 | **반사실 코칭** (`/api/coach`) | ✅ | **③ 지시 — 무엇을 했다면 승률이 얼마** |
| F5 | 소환사 최근 경기 판정 | ⏸ 키 대기 | **1차 타겟의 핵심 기능** |
| F6 | 판정 요약 타일 (역전패 n판…) | ⏸ 키 대기 | 첫 화면이 진단부터 던진다 |
| F7 | 챔피언별 리드 전환율 | 📋 미착수 | 5장 |
| F8 | 시점별 승률 곡선 (분 단위) | 📋 구상만 | `plan.md` 7장 |

F5·F6 은 서버가 이미 내려준다 (`/api/summoner` 의 `verdict`·`summary`).
**막힌 것은 코드가 아니라 Riot API 키 하나다.**

## 4. 실시간 승률은 왜 못 하나

모델은 10분 시점의 골드·킬·CS 차이를 요구한다. 그런데

- `spectator-v5`(진행 중 경기) → 참가자·챔피언·밴·시작 시각만. **실시간 수치 없음**
- `match-v5` 타임라인 → 그 값들이 있지만 **경기가 끝난 뒤에만** 열림
- Live Client Data API(`127.0.0.1:2999`) → 값은 있지만 **게임이 도는 본인 PC 에서만**

즉 실시간은 웹 서비스가 아니라 **PC 에 설치하는 오버레이**여야 한다.
지금 범위 밖이므로 **끝난 경기 복기**로 선을 그었다.

## 5. 챔피언별 리드 전환율 (F7) — 검토

### 무엇을 만들 것인가

전적 사이트의 챔피언 승률표는 이미 있다. 우리가 더할 것은 **판정을 챔피언별로 쪼개는 것**이다.

> "이 챔피언은 10분 리드를 **굴리는가, 놓치는가**"

| 지표 | 뜻 | 이 챔피언은 |
|---|---|---|
| 리드 전환율 = 우세승 / (우세승 + 역전패) | 유리하게 시작한 판을 닫는 비율 | 높으면 **스노우볼형** |
| 회복률 = 역전승 / (역전승 + 열세패) | 불리해도 살아나는 비율 | 높으면 **후반 캐리형** |

챔피언 승률(예: 52%) 하나로는 안 보이는 것이 갈린다 — 같은 52% 라도
"초반에 벌어 끝내는 챔피언"과 "초반을 버티고 뒤집는 챔피언"은 완전히 다르다.
**이건 10분 시점 판정이 있어야만 계산된다.**

### 데이터 — 그마~챌린저 수집

현재 데이터셋(Kaggle 다이아 10분 스냅샷)에는 **챔피언 컬럼이 아예 없다.**
따라서 Riot API 로 새로 모아야 한다.

```
league-v4 (challenger/GM/master 엔트리)  →  puuid 수천 개
  → match-v5 match ids (큐 420 솔로랭크)
    → match 상세      : championName · teamPosition · win     (1 요청)
    → match timeline  : 10분 시점 13개 지표                    (1 요청, 응답이 크다)
```

**비용 추정** (개발용 키 기준: 100요청 / 2분)

| 목표 | 요청 수 | 예상 시간 |
|---|---|---|
| 챔피언×라인 승률만 (타임라인 불필요) | 약 5,500 | 약 2시간 |
| **판정까지 (타임라인 필요)** | 약 10,500 | **약 3.5~4시간** |

### 결정하기 전에 알아야 할 것 3가지

1. **개발용 키는 24시간마다 죽는다.** 4시간짜리 수집이 중간에 끊기므로
   **이어서 받는(resumable) 수집기**가 필수다. 받은 것을 즉시 파일에 append 하고,
   이미 받은 match id 는 건너뛰는 구조여야 한다.
2. **모델을 다시 학습하는 게 아니다.** 챔피언을 피처로 넣자는 것이 아니라,
   기존 모델의 판정을 챔피언별로 **집계**하는 것이다. 그래서 `artifacts/` 는 그대로다.
   (챔피언은 0분 정보라 10분 모델의 피처로는 적합하지 않다 — `plan.md` 7-2 참고)
3. **티어가 바뀌면 분포가 바뀐다.** 그마~챌 표본으로 만든 값을
   다이아 이하 유저에게 그대로 보여주면 안 된다. **"그마~챌 기준"** 을 화면에 명시해야 한다.

### 우선순위

F7 은 **F5·F6 다음**이다. 둘 다 같은 키를 기다리는데, F5·F6 은 이미 서버가
계산까지 끝냈고 F7 은 수집기부터 새로 짜야 한다. 키가 살아나면
**F5·F6 을 먼저 눈으로 확인하고**, 그다음 수집을 건다.
