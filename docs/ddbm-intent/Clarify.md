[명확화된 결정 — 전부 닫힘]
Q1. 15분 데이터가 팀 DB에 없다 → 8/31 저녁 Oracle's Elixir 2022 프로 경기 데이터를 확보해 실험 B로 해결. 랭크(A)와 프로(B)는 절대 수치를 비교하지 않고, B 안에서 10분 vs 15분만 비교한다
Q2. 목표 정확도 → 0.70 확정 + 0.75 도전 병기. 실측 천장 약 0.73이라 개선 작업은 2시간 상한, 남는 시간은 해석·문서에 투자
Q3. 경기 유형 프로파일 → 정식 범위 포함 (관전 가이드·오류 분석과 연결)
Q4. 웹스택 → Vue/Express 대신 Flask (8/31). predict.py 직접 import로 화면·모델 패리티를 구조적으로 보장
Q5. Riot API (9/1) → 끝난 경기 복기만 구현. 실시간 연동은 공식 API 미제공이라 범위 밖. 키는 환경변수로만
Q6. 이름 규칙 (8/31) → 발표 주제 "LoL 경기는 언제 승부가 결정되는가?" / 시스템 이름 "LoL 승패 예측·설명 서비스"를 산출물·코드·DB 뷰에 일관 적용

[엣지 케이스 정리]
· 입력이 학습 범위(1%~99%) 밖 → 예측은 하되 warnings에 "학습 범위 밖 입력" 표시 (schema.json 근거)
· 골드차 ±1,000 접전 구간 → "예측 신뢰도 낮음" 경고 병기. 화면 확률은 모델 predict_proba만 쓴다 (구간별 승률 표를 규칙으로 쓰면 'ML 전용 구현' 원칙 위반)
· DB 접속 불가 → 같은 계산식의 CSV 폴백. 비밀번호는 환경변수 DB_PASSWORD로만
· 같은 시드·다른 행 순서 → 분할이 달라진다. gameId 정렬 후 분할 (재현성 8번째 규약)

[우선순위]
1 예측 정확도·안정성(SC-1·2) → 2 해석 일치(SC-3) → 3 오류 분석(SC-4) → 4 산출물·재현(SC-5) → 5 시점 비교(SC-6) → 6 스모크 테스트(SC-7, 부분 통과 — 킬 오답으로 부록 보완)
