# Study — 이 프로젝트에 쓰인 개념, 이해부터 원리까지

> 우리가 공부하고, 남에게 설명할 수 있게 되기 위한 가이드.
>
> **개념마다 "왜 이걸 하는데?"부터 시작한다.** 그 질문에 답이 안 되면 원리를 외워도 소용이 없다.
> 구조는 늘 같다 — **왜 하나 → 원리 → 우리 프로젝트에서 → 발표 한 줄**.
> 질문받았을 때 "발표 한 줄"만 외우지 말고 "왜"까지 말할 수 있어야 한다.

**이 파일과 다른 파일의 관계**

| 알고 싶은 것 | 볼 파일 |
|---|---|
| 개념이 무엇이고 왜 하는가 | **이 파일** |
| 우리가 실제로 뭘 했고 결과가 뭔가 | [README.md](README.md) |
| 그 숫자의 근거 파일이 어디인가 | [reports/metrics_summary.md](reports/metrics_summary.md) |
| 모델을 쓸 때 조심할 것 | [model_card.md](model_card.md) |

README를 읽다가 개념이 막히면 이 파일의 해당 번호로 오면 된다.

---

## 1. 지도학습과 이진분류

**왜** — "10분 상태를 보고 승패를 맞혀라"를 컴퓨터가 풀 수 있는 형태로 바꿔야 한다.

**원리** — 지도학습은 (입력, 정답) 쌍을 잔뜩 보여주고 입력→정답의 패턴을 찾게 하는 것.
정답이 두 가지(승/패)뿐이면 이진분류다. 컴퓨터는 규칙을 외우는 게 아니라,
"골드차가 이 정도면 승리 쪽 확률이 이 정도"라는 **경향**을 숫자(가중치)로 배운다.

**우리** — 입력 = 10분 시점 차이 피처 13개, 정답 = `blueWins`(승 1 / 패 0).
무승부가 없는 게임이라 이진분류가 깔끔하게 성립한다.

**발표 한 줄** — "과거 경기 9,879판으로 패턴을 배워, 처음 보는 경기의 승패를 맞힙니다."

## 2. 기준선(베이스라인) — 찍기 점수

**왜** — "정확도 74%"는 그 자체로는 좋은지 나쁜지 알 수 없다. 비교 대상이 필요하다.

**원리** — 아무것도 배우지 않은 예측기(다수 클래스로만 찍기)의 점수를 먼저 잰다.
승패가 반반이면 찍기도 50%는 맞는다. 모델의 가치 = 모델 점수 − 찍기 점수.
만약 데이터가 9:1로 치우쳐 있었다면 찍기만 해도 90%라, 90% 모델은 아무 가치가 없다.

**우리** — 찍기 0.5009 (DummyClassifier 실측). 최종 0.7394 → **+23.8%p가 진짜 성과**.

**발표 한 줄** — "찍어도 50%는 맞기 때문에, 저희는 항상 '찍기 대비 몇 점 올랐나'로 말합니다."

## 3. 훈련/시험 분할과 누수(leakage)

**왜** — 배운 데이터로 시험을 보면 점수가 가짜로 높게 나온다. 실력은 처음 보는 데이터에서만 잰다.

**원리** — 데이터를 문제집(train)과 시험지(test)로 나누고 시험지는 봉인한다.
**누수**란 시험지의 정보가 어떤 경로로든 학습 과정에 새어 드는 것.
예: 전체 데이터로 평균·표준편차를 구해 표준화한 뒤 분할하면, 시험지의 통계가
훈련에 이미 반영된 것 — 에러 없이 점수만 올라서 제일 무서운 실수다.
그래서 **분할이 모든 전처리보다 먼저**여야 한다.
**층화(stratify)** 분할은 나눌 때 승/패 비율을 양쪽에서 똑같이 유지하는 것.

**우리** — 학습 7,903 / 시험 1,976 (층화, 시드 42). 시험지는 모델·피처 선택에 쓰지 않고
최종 평가와 해석에만 사용했다.

**발표 한 줄** — "시험지를 미리 보면 점수는 오르지만 실력이 아닙니다. 그래서 분할을 제일 먼저 했습니다."

## 4. 로지스틱 회귀 — 우리의 최종 모델

**왜** — 확률(0~100%)을 내면서, 왜 그렇게 예측했는지 읽을 수 있는 모델이 필요했다.

**원리** — 두 단계다.
① 피처를 가중치로 곱해 더한다: z = w₁x₁ + w₂x₂ + … + b (선형결합 — 일종의 종합 점수)
② 그 점수를 **시그모이드 함수** σ(z) = 1/(1+e^(−z)) 에 넣어 0~1 확률로 변환한다.
z=0이면 50%, z가 크면 100%에, 작으면 0%에 가까워지는 S자 곡선이다.
학습이란 훈련 데이터의 예측 확률이 정답과 최대한 맞도록 가중치 w를 조정하는 과정.
**가중치의 부호와 크기가 곧 해석이다**: w가 양수면 그 피처가 클수록 블루 승리 쪽.

**우리** — EDA에서 골드차별 실제 승률이 S자 곡선이었다(eda1 그림). 데이터 모양 자체가
시그모이드라 로지스틱이 잘 맞는 구조였다. 표준화 계수: 골드차 +1.231, 경험치차 +0.461, 드래곤 +0.267.

**발표 한 줄** — "피처마다 가중치를 배우고 그 합을 확률로 바꾸는 모델이라, 가중치를 읽으면 '왜'가 나옵니다."

## 5. 표준화(StandardScaler) — 왜 하고, 왜 Pipeline 안인가

**왜** — 골드차는 수천 단위, 드래곤차는 ±1이다. 단위가 다르면 가중치끼리 비교할 수 없다.

**원리** — 각 피처를 (값 − 평균) ÷ 표준편차로 바꾸면 전부 "평균 0, 편차 1" 스케일이 된다.
그러면 계수의 크기가 "그 피처가 1 표준편차만큼 움직일 때의 영향력"으로 통일돼 서로 비교 가능.
단, 평균·표준편차는 **훈련 데이터에서만** 배워야 한다(3번 누수). 이를 코드로 보장하는 게
**Pipeline** — 교차검증의 각 fold 안에서도 스케일러가 그 fold의 훈련 부분으로만 fit 된다.

**우리** — `Pipeline([("scaler", StandardScaler()), ("model", LogisticRegression())])`.
저장할 때도 이 전체를 통째로 저장해, 서비스에서 같은 변환이 보장된다.

**발표 한 줄** — "단위를 맞춰야 가중치를 비교할 수 있고, 그 변환을 Pipeline 안에 넣어야 누수가 없습니다."

## 6. 과적합 — "문제집 암기"

**왜** — 훈련 점수만 보면 랜덤포레스트가 만점(1.0000)이었다. 왜 그걸 안 골랐는지 설명해야 한다.

**원리** — 모델이 패턴 대신 **훈련 데이터 자체를 외우면**, 훈련 점수는 높고 새 데이터에선 떨어진다.
진단법: **학습 점수와 검증 점수를 나란히 본다.** 격차가 크면 암기다.
복잡한 모델일수록(나무 수천 개 등) 외울 용량이 커서 과적합하기 쉽다.

**우리** — 랜덤포레스트: 학습 1.0000 / 검증 0.7191 → 격차 +0.2809 (기준 0.03의 9배) → 탈락.
로지스틱: 격차 +0.0047 → 사실상 과적합 없음. 8종 비교에서 성능이 복잡도의 역순이었다 —
이 데이터의 정보량이 적어서(피처 2개가 전체와 동급) 복잡한 모델이 외울 것밖에 없었기 때문.

**발표 한 줄** — "학습 점수 만점은 잘한 게 아니라 문제집을 외웠다는 뜻입니다. 격차를 봐야 합니다."

## 7. k-fold 교차검증과 시드 반복

**왜** — 시험 한 번의 점수는 운일 수 있다. "그 점수가 흔들리지 않는가"를 재야 한다.

**원리** — 훈련 데이터를 k조각(우리는 5)으로 나누고, 4조각으로 공부 + 1조각으로 시험을
5번 돌아가며 반복 → 점수 5개의 **평균 ± 표준편차**로 보고한다. 표준편차가 작으면 안정적.
층화 버전(StratifiedKFold)은 각 조각에서도 승/패 비율을 유지한다.
한 단계 더 나가면 **분할 자체를 바꿔**(시드 10개) 전체를 반복 — 분할 운까지 제거한 분포가 나온다.

**우리** — 5-fold: 0.7315 ± 0.0153 (기준 std<0.02 통과). 시드 10회: 0.7366 ± 0.0081,
10회 전부 하한 0.70 통과, 승리요인 순위도 10회 모두 동일.

**발표 한 줄** — "5조각으로 5번 시험 보고, 분할을 바꿔 10번 더 반복해서 '운이 아님'을 확인했습니다."

## 8. 평가지표 — 정확도, 혼동행렬, F1, AUC, Brier

### 8-0. 그 전에 — 왜 지표가 여러 개나 필요한가?

**"맞힌 비율 하나면 충분하지 않나?"** 라는 생각이 먼저 들 것이다. 예를 들어 본다.

병을 진단하는 모델이 있다. 1,000명 중 실제 환자는 10명뿐이다.
이 모델이 **"전원 건강합니다"** 라고만 답하면 정확도는 **99%** 다. 그런데 환자를 **한 명도** 못 찾았다.

**정확도 99%짜리 쓸모없는 모델**이 나오는 것이다.
정확도는 **"얼마나 맞혔나"만 말하고 "어떻게 틀렸나"는 감춘다.**
그래서 지표가 여러 개 필요하다. 각각 **다른 질문**에 답하기 때문이다.

| 지표 | 답하는 질문 |
|---|---|
| 정확도 | 전체 중 몇 개나 맞혔나? |
| 혼동행렬 | **어느 방향으로** 틀렸나? |
| 정밀도·재현율·F1 | 한쪽 실수가 더 아플 때, 그쪽을 잘 막았나? |
| AUC | 커트라인을 옮겨도 여전히 쓸 만한가? |
| Brier | "73%"라고 말한 게 **정말 73%**였나? |

### 8-1. 혼동행렬 — 왜 하나?

**왜** — 정확도 0.74는 **"100판 중 74판 맞혔다"** 만 알려준다.
그런데 못 맞힌 26판이 **어떤 식으로** 틀렸는지는 안 나온다. 두 가지가 완전히 다른데도 말이다.

- 블루가 **이긴다고 했는데 진** 경우 (헛다리)
- 블루가 **진다고 했는데 이긴** 경우 (놓침)

이게 한쪽으로 쏠려 있으면 **모델이 특정 팀 편을 드는 것**이고, 그건 고쳐야 할 문제다.
정확도만 보면 이 쏠림이 안 보인다. **그래서 네 칸으로 쪼개 센다.**

**원리** — 실제(승/패) × 예측(승/패) = 2×2, 네 칸.

|  | 패배 예측 | 승리 예측 |
|---|---|---|
| **실제 패배** | TN 맞힘 | FP 헛다리 |
| **실제 승리** | FN 놓침 | TP 맞힘 |

대각선(TN·TP)이 맞힌 것, 나머지 둘이 틀린 것이다. 정확도 = 대각선 ÷ 전체.

**우리** — TN 746 · FP 244 / FN 271 · TP 715.
**틀린 방향이 244 대 271로 거의 같다.** 어느 팀도 편들지 않는다는 뜻이라 임계값을 손댈 이유가 없었다.
(→ README **페이즈 1의 결과**에 그림)

### 8-2. 정밀도·재현율·F1 — 왜 하나?

**왜** — 위의 두 실수는 **아픈 정도가 상황마다 다르다.**
암 진단이라면 "놓침(FN)"이 치명적이고, 스팸 필터라면 "헛다리(FP)"가 더 밉다(정상 메일이 버려지니까).
**어느 쪽 실수를 더 막아야 하는지**를 재려면 정확도로는 안 된다.

**원리** — 정밀도 = 승리라 한 것 중 진짜 비율(헛다리를 덜 냈나).
재현율 = 실제 승리 중 잡아낸 비율(덜 놓쳤나). **F1** = 둘의 조화평균.
데이터가 한쪽으로 치우쳤을 때 정확도 대신 쓴다.

**우리** — 승패가 50:50이고 어느 쪽으로 틀려도 손해가 같아서 **정확도를 1순위로 썼다.**
F1(0.7352)도 정확도와 거의 같게 나와 문제가 없음을 확인했다.

### 8-3. AUC — 왜 하나?

**왜** — 정확도에는 **숨은 가정**이 있다. "확률 0.5 넘으면 승리"라고 자른다는 가정이다.
그 커트라인을 0.4나 0.6으로 옮기면 **정확도도 같이 바뀐다.**
즉 정확도는 **커트라인 하나에서만 잰 점수**다. 커트라인을 잘 골라서 좋아 보이는 것일 수도 있다.

**원리** — AUC는 **커트라인을 0부터 1까지 전부 옮겨가며** 성능을 재고 그 아래 넓이를 구한다.
0.5면 찍기, 1.0이면 완벽. 쉬운 해석: **"승리 경기와 패배 경기를 하나씩 뽑았을 때
승리 쪽에 더 높은 확률을 줄 가능성."**

**우리** — AUC 0.8150. 커트라인을 어디로 옮겨도 성능이 유지되므로 **0.5를 그대로 써도 된다**는 근거가 됐다.

### 8-4. Brier — 왜 하나?

**왜** — 우리 서비스는 **"73%"라는 숫자를 사용자에게 보여준다.**
그런데 정확도와 AUC는 **순서만** 본다 — 승리 쪽이 더 높기만 하면 통과다.
그러면 실제로는 55%인 경기를 **95%라고 말해도** 두 지표는 만점이 나올 수 있다.
**확률 숫자 자체가 믿을 만한지**는 따로 재야 한다.

**원리** — (예측 확률 − 실제 결과)²의 평균. **낮을수록 좋다.**
"70%라고 말한 경기들이 실제로 70% 정도 이겼는가"를 재는 것이다.

**우리** — Brier 0.176. 확률을 그대로 화면에 띄워도 되는 수준이라 판단했다.

**발표 한 줄** — "승패가 반반이고 어느 쪽으로 틀려도 손해가 같아서 정확도를 1순위로 썼고,
확률을 화면에 보여주기 때문에 Brier로 확률 자체도 따로 검증했습니다."

## 9. 피처 공학 — 중복 제거와 차이 피처

**왜** — 원본 38개 컬럼에는 같은 정보가 여러 번 들어 있었다. 그대로 두면 해석이 망가진다.

**원리** — 완전 중복(상관 ±1.0)은 새 정보가 0이면서 계수를 둘로 쪼개 해석을 왜곡한다.
쌍 검사로 9개를 찾았지만 **세 컬럼의 합 관계**(EliteMonsters = Dragons + Heralds)는
쌍 상관에 안 걸린다 — 완전한 선형 종속은 계수가 유일하게 정해지지 않게 만든다(VIF 무한대).
남은 피처를 "블루 − 레드" **차이값**으로 합치면: 피처 수 절반, 모든 축이 "양수 = 블루 우세"로
통일, 게임의 본질(격차 싸움)을 직접 표현.

**우리** — 38 → 27(중복 제거) → 13(차이 피처). 성능도 0.7334 → 0.7369로 소폭 상승
(잡음 제거 효과). 규칙: 피처 계산식은 SQL 뷰 한 곳에만 두고 파이썬은 읽기만.

**발표 한 줄** — "레드팀 골드차는 블루팀 골드차에 마이너스만 붙인 거울이라, 이런 중복 11개를 지우고 차이값 13개로 만들었습니다."

## 10. 다중공선성 — 킬 계수가 음수인 이유

**왜** — 킬은 단독으로 보면 강한 신호인데 모델 계수는 음수다. 설명 못 하면 발표에서 무너진다.

**원리** — 두 피처가 강하게 상관되면(킬↔골드 +0.92), 모델 계수는 "**다른 피처를 고정한 채**
이 피처만 1 움직일 때의 효과"가 된다. 골드가 킬의 정보를 거의 다 담고 있으므로
킬에게는 **남는 몫이 거의 없고(계수 크기 0.107), 그 작은 값의 부호는 신뢰할 수 없다.**
이게 다중공선성이다 — 값이 커지는 게 아니라 **불안정해진다**(킬의 VIF 14.07).
VIF는 **"그 지표를 나머지 지표들로 얼마나 설명할 수 있나"** 를 잰 값이다. 10을 넘으면 중복에 가깝다고 본다.

단독 영향력은 **효과크기(Cohen's d)** = (승리군 평균 − 패배군 평균) ÷ 합동 표준편차로 본다.
같은 값이 "단독으론 강하고(효과크기 1.09) 통제 후엔 거의 없음(계수 −0.11)"일 수 있다 — 모순이 아니다.

**증명** — 페이즈 2에서 골드를 빼고 다시 학습해봤다. **킬 계수가 −0.107 → +0.508로 뒤집혔고**,
같이 눌려 있던 미니언·어시스트·타워·정글 몹도 전부 양수가 됐다. 정확도는 0.7394 → 0.7373으로
**0.002밖에 안 떨어졌다.** 음수는 킬의 성질이 아니라 **골드에 가려진 결과**였다는 직접 증거다.

> 흔한 오답 — "골드가 같은데 킬만 많은 건 돈으로 못 굴린 경기라 불리해서 음수다."
> 그럴듯하지만 **틀렸다.** 그게 진짜 효과라면 골드를 빼도 음수여야 하는데, 빼면 +0.508이 된다.

**우리** — 정확한 해석: **"킬은 골드로 환산됐을 때만 의미가 있다."**
사용자 화면에는 계수가 아니라 효과크기 순위를 쓴다("킬하면 진다" 오해 방지).

**발표 한 줄** — "킬로 번 돈이 이미 골드차에 들어 있어서, 골드를 본 뒤에는 킬이 더 보탤 정보가 없습니다."

## 11. permutation importance — 두 번째 해석 방법

**왜** — 계수 하나만 믿으면 그 방법의 편향일 수 있다. 성격이 다른 방법으로 교차 확인해야 한다.

**원리** — 학습이 끝난 모델을 두고, 시험 데이터에서 **한 피처의 값만 무작위로 뒤섞은 뒤**
정확도가 얼마나 떨어지는지 잰다. 중요한 피처일수록 섞었을 때 손해가 크다.
계수와 달리 모델 내부를 보지 않고 "실제 예측에 얼마나 기여하나"를 직접 실험하는 방식.

**우리** — 계수 순위와 permutation 순위가 골드>경험치>드래곤으로 **완전 일치**.
4위 이하는 permutation이 0 근처라 순위 자체가 무의미 → "유의미한 요인은 3개뿐"이라고 보고.

**발표 한 줄** — "그 피처를 못 쓰게 섞어버렸을 때 점수가 얼마나 떨어지는지로 중요도를 쟀고, 계수와 같은 답이 나왔습니다."

## 12. PCA — 차원을 줄여 구조를 본다

**왜 하나?** — 지표가 13개라고 해서 **정보도 13개인 것은 아니다.**

극단적인 예로, 키를 `cm`로 한 번, `inch`로 한 번 넣으면 컬럼은 2개지만 정보는 1개다.
우리 데이터도 골드·경험치·킬·레벨이 서로 0.9씩 붙어 있으니 **비슷한 상황**이다.

그래서 묻고 싶은 것은 **"이 13개가 사실은 몇 개짜리 정보인가?"** 다.
답이 "사실 1~2개"라면, **지표를 더 넣어도 성능이 안 오르는 이유**가 설명된다.
PCA는 이 질문에 답하는 도구다.

**원리** — 데이터가 **가장 넓게 퍼진 방향**을 찾아 첫 축(PC1)으로 삼고,
그다음으로 넓게 퍼진 방향을 PC2로 삼는 식으로 좌표를 **회전**시킨다.
그러면 앞쪽 축 몇 개에 정보가 몰리게 되고, **각 축이 전체의 몇 %를 설명하는지** 숫자로 나온다.

주의할 점 — 새 축은 원래 지표들을 섞어 만든 것이라 **이름이 없다.**
"PC1이 0.8"이라고 해봐야 사용자에게 설명할 수가 없다.
그래서 우리처럼 **"왜"를 설명해야 하는 프로젝트에서는 예측 입력으로 쓰지 않는다.**

**우리** — 13개에 돌리니 **PC1 하나가 38.9%**를 설명했고, 그 축을 만드는 것은
골드·경험치·킬·레벨·어시스트·CS **6개가 전부**였다. 이름을 붙이면 **"성장 격차 축"**이다.
**"피처 2개가 전체 13개를 이긴다"는 결과의 구조적 이유**가 여기 있다.

한편 **군집을 나눌 때 쓴 PCA는 입력이 다르다**(중립 피처 5개, 32.0%/20.4%) — 같은 그림으로 착각하지 말 것.

**한계도 정직하게** — PCA + KMeans(k=2)로 나눠보니 그냥 "블루 우세 / 레드 우세"로 갈렸다.
정답을 되비춘 것이라 새 정보가 없었다. **골드를 빼고 다시 해도 마찬가지였다**
(일치율 72.6% → 72.3%). 성장 지표 전체가 한 덩어리라 하나 뺀다고 달라지지 않는다.
(→ README **페이즈 2-3**)

**발표 한 줄** — "13개처럼 보여도 실제 정보는 '성장 격차' 한 축이 39%라, 피처를 늘려도 성능이 안 오릅니다."

## 13. KMeans 군집화와 실루엣 — 경기 유형 나누기

**왜 하나?** — 지금까지는 전부 **"누가 이기나"** 를 물었다. 정답(승/패)을 알려주고 맞히게 하는 방식이다.

그런데 정답을 알려주지 않고 **"비슷한 것끼리 알아서 묶어봐"** 라고 시키는 방법도 있다.
이걸 비지도학습이라 한다. 왜 이걸 하냐면, **우리가 미처 생각 못 한 분류가 나올 수 있기 때문**이다.

우리가 묻고 싶었던 건 이것이다 — **"이 판은 어떤 성격의 경기였나?"**
난타전이었나, 조용한 운영전이었나. 이건 승패와는 다른 질문이고, 정답표가 없다.
그래서 군집화를 쓴다.

**원리** — KMeans는 점들을 k개 무리로 나누되 **무리 안에서 서로 가깝도록** 만든다.
① 각 점을 가장 가까운 중심에 배정 → ② 중심을 그 무리의 평균으로 옮김 → 안 변할 때까지 반복.

거리로 재기 때문에 **표준화가 필수**다. 안 하면 골드(단위 수천)가 킬(단위 한 자리)을 완전히 눌러버린다.

**실루엣 점수**는 "잘 나뉘었나"를 재는 숫자다. 1에 가까우면 무리가 뚜렷하고, 0이면 경계가 흐릿하다.
k를 몇으로 할지 고를 때 참고한다.

### 묶는 모델도 네 가지가 있다 — 왜 하필 KMeans인가?

"비슷한 것끼리 묶어라"를 시키는 방법이 KMeans 하나가 아니다. 방식이 다른 네 가지를 같은 조건에서 겨뤘다.

**KMeans — "중심에서 가까운 순서로 묶는다."**
무리마다 중심점을 두고, 각 경기를 가장 가까운 중심에 배정한다.
중심이 곧 그 무리의 **평균값**이라 "운영전 = 킬 9회·CS 456" 같은 표가 바로 나온다.
→ 실루엣 유일하게 0.2 위, **채택.**

**GMM(가우시안 혼합) — "확률로 묶는다."**
경기 하나를 "운영전일 확률 60%, 난타전일 확률 40%"처럼 **소속 확률**로 배정한다.
경계가 흐린 데이터에 유리하지만, 우리 데이터에서는 그 흐린 경계 때문에
실루엣이 0.05~0.17로 낮았다. → 탈락.

**계층적(Agglomerative) — "가까운 것끼리 차례로 합친다."**
경기 하나하나를 무리 하나로 시작해서, 제일 가까운 두 무리를 반복해 합쳐 나간다.
전 과정이 나무 모양으로 남아 "몇 개로 자를지"를 나중에 정할 수 있는 게 장점.
대신 모든 쌍의 거리를 재야 해서 **메모리를 많이 먹는다**(7,903판 전체가 안 돌아 4,000표본으로).
같은 4,000표본에서 KMeans와 5번 재대결해 **5번 다 졌다**(0.14~0.20 vs 0.21). → 탈락.

**DBSCAN — "빽빽한 곳을 한 무리로 본다."**
점이 빽빽하게 모인 지역을 무리로 잡고, 어디에도 안 끼는 점은 **"잡음"으로 버린다.**
모양이 제멋대로인 무리를 잘 찾지만, 우리 경기 데이터는 밀도가 뚝 끊기는 데 없이
연속적이라 **경기의 10%(784판)를 잡음으로 버렸다.** 경기를 버리면 유형 분류가 아니다. → 탈락.

**요점** — 예측 모델을 8종 겨루고 뽑았듯 군집 모델도 겨루고 뽑아야 한다.
KMeans가 이긴 이유는 점수(실루엣)만이 아니라 **중심을 표로 설명할 수 있어서**다 —
우리 프로젝트의 목표가 "왜까지 설명"이니까. (→ README **페이즈 2 보충 2-4**)

**우리가 따로 한 것 — 진영 정보 지우기.** 그냥 묶으면 군집이 "블루 우세 / 레드 우세"로 갈린다.
그건 정답을 되비춘 뻔한 결과라 새로 아는 게 없다. 그래서 **절대값과 양 팀 합만** 써서
"누가 앞서는지"는 지우고 **"얼마나·어떤 식으로"** 만 남겼다.

**우리** — 4종 비교에서 KMeans를 채택했고(위), 시드를 바꿔도 유형 배정이 **99% 이상 재현**됐다.
산점도로 보면 **'일방적 경기'만 가로축(골드 기울기)에서 뚜렷이 떨어져 있고**, 나머지 셋은
세로축(킬 수)으로만 갈린다 — 격차가 아니라 스타일 차이라는 뜻이다. (→ README **페이즈 1**)
4유형이 나왔다: 운영전 41% / 난타전 32% / 일방적 19% / 시야전 7%.
**단, 실루엣이 0.203~0.219로 전부 비슷해 k를 정해주지 못했다.**
**k=4는 해석하기 좋아서 고른 값**이지 데이터가 자연히 4덩어리라는 뜻이 아니다.
발표에서 이걸 숨기지 않고 한계로 밝힌다. (→ README **페이즈 1**)

**발표 한 줄** — "누가 이기는지를 지운 좌표에서 경기를 묶으니 4가지 유형이 나왔고, 일방적 유형만 10분에 결판이었습니다."

## 14. 재현성 — 시드만으로는 부족했다

**왜** — 남이 돌려서 같은 결과가 나와야 "검증된 분석"이다. 우리가 직접 사고를 겪었다.

**원리** — 무작위가 들어가는 모든 곳(분할, fold 섞기, 모델 초기화)에 시드를 고정한다.
그런데 `train_test_split`과 `StratifiedKFold(shuffle=True)`는 **시드가 같아도 입력 행 순서가
다르면 결과가 다르다.** 데이터를 읽는 경로가 둘(DB/CSV)이면 정렬까지 통일해야 한다.

**우리** — DB는 gameId 정렬로 읽고 CSV는 정렬 없이 읽어서, 같은 시드에 다른 분할이 생겨
점수가 문서 기록과 다르게(0.7136) 나온 적이 있다. 정렬을 통일해 해결했고, 규약에 "행 정렬 고정"을 추가했다.
그리고 이 사건이 준 교훈대로, 단일 분할 값 대신 시드 10회 반복 분포(0.7366±0.0081)로 보고한다.

**발표 한 줄** — "시드 고정만 믿었다가 입력 순서 때문에 점수가 달라지는 걸 직접 겪었고, 규약에 '행 정렬 고정'을 추가했습니다."

---

## 15. 나이브베이즈 — 정확도는 비슷한데 왜 탈락했나

**왜 하나?** — 모델 비교에서 로지스틱을 뽑았으면, **"차점자는 왜 안 됐는데?"** 라는 질문에
답할 수 있어야 한다. 점수 2위이자 외우지 않은 후보였던 나이브베이즈를 끝까지 학습해서 확인했다.

**원리** — "승리한 경기에서 이 지표가 이 값일 법한 정도"를 **지표마다 따로 재서 전부 곱한다.**
곱셈만 하면 되니 빠르고 단순하다. 이름의 나이브(naive, 순진)는
**"지표끼리 서로 독립"이라고 순진하게 가정**한다는 뜻이다.

**문제는 그 가정이 우리 데이터에서 깨진다는 것** — 골드·경험치·킬은 상관 0.9로 얽혀 있다(10번).
셋 다 "성장 격차"라는 같은 증거인데 독립인 줄 알고 따로따로 곱하니,
**같은 증거를 여섯 번 세는 셈**이다. 51%짜리 확신이 거듭 곱해져 99%로 부푼다.

**우리** — 정확도는 0.7313으로 로지스틱(0.7394)과 비슷했다. 그런데 확률의 얼굴이 달랐다.

| | 정확도 | Brier | "95% 이상/5% 이하" 확신 |
|---|---|---|---|
| 로지스틱 | 0.7394 | 0.1756 | 4.9% |
| 나이브베이즈 | 0.7313 | 0.2179 | **63.6%** |

경기 3판 중 2판에 극단 확신을 건다 — 팽팽한 접전에도. 분할을 10번 바꿔도 로지스틱이 10번 다 이겼다.
우리 서비스는 확률을 화면에 그대로 보여주므로 탈락. 승/패 분류만 필요했다면 쓸 만했을 것이다.

> 흔한 오해 — "정확도가 비슷하면 아무 모델이나 써도 된다."
> 아니다. **분류가 같아도 확률의 품질은 다르다.** 확률을 보여주는 서비스라면 Brier 를 봐야 한다(8-4번).

**발표 한 줄** — "나이브베이즈는 지표가 독립이라 가정하는데 우리 지표는 상관 0.9라, 같은 증거를 중복해 곱해 확률을 부풀립니다."

## 16. 2단계 라우팅(전문가 위임) — 해봤는데 왜 안 됐나

**뭔가** — 1차 모델이 애매해하면(확률 0.4~0.6) 판정을 보류하고, 그 구간만 따로 배운
2차 '전문 모델'에 넘기는 설계. 병원에서 일반의가 애매한 환자를 전문의에게 보내는 것과 같다.
접전 구간을 이 구조로 보완하자는 제안이 있어 실측했다.

**왜 기대하게 되나** — 경기 유형(군집)이라는 맥락을 주면, "후반 지향 조합의 −500골드는
사실 접전이 아니다" 같은 판단이 가능해 보이기 때문.

**우리 실측** (`src/twostage_check.py`) — 2차 후보 4종 전부 1차 단일 모델보다 나빴다.
유형 원핫을 줘도 예측이 한 판도 안 바뀌었고(계수 ≈ 0), 분할을 바꿔 5회 반복해도 4회 손해.

**왜 안 되나** — 이 구조가 통하려면 2차 모델이 1차가 못 보는 **새 정보**를 가져야 한다.
그런데 경기 유형은 골드·킬·와드의 재조합이라 새 정보가 아니고, 2차는 같은 13개 지표를
**더 적은 데이터**(애매한 1,721판)로 볼 뿐이다. 애매한 경기는 모델이 잘못 고른 경기가 아니라
정보가 부족한 경기라서, 구조를 바꿔도 없는 단서가 생기지 않는다.
접전의 해법은 시간이다 — 15분 시점이면 접전만 +7.8%p 오른다(페이즈 3).

**주의(누수)** — 이런 실험을 할 때 2차 학습용 '애매한 경기'를 1차 모델이 자기 학습 데이터에
낸 확률로 고르면 안 된다. 자기 데이터엔 과하게 자신만만해서 애매 구간이 실제보다 좁게 잡힌다.
교차검증 밖(OOF) 확률로 골라야 한다.

## 17. 상관과 인과 — "했다면"은 어디까지 참인가

**뭔가** — 함께 움직이는 것(상관)과, 하나가 다른 하나를 **만드는** 것(인과)은 다르다.
"골드차가 큰 팀이 이기더라"는 데이터가 말해주지만, 그것만으로는 세 가지를 못 가른다 —
골드가 승리를 만드는지, **이길 팀이라서 골드를 버는지**(방향이 반대),
실력이라는 제3의 것이 둘 다 만드는지(공통 원인).

**우리 프로젝트에서 이 구분이 걸리는 곳 세 군데**

1. **승리요인 순위** — "무엇이 승패를 가르나"는 관계의 기술이지 인과 증명이 아니다.
   그래서 문서 전체에서 "가른다 · 관련 있다"로 쓰고, "만든다 · 일으킨다"는 쓰지 않는다.
2. **골드 흐름 도식** — 예외가 하나 있다. "킬을 하면 돈을 받는다"는 상관에서 읽어낸 게
   아니라 **게임 규칙이 보증하는 방향**이다. 데이터가 못 주는 인과의 방향을 도메인 지식이
   줄 때만 화살표를 그렸다 (킬·CS → 골드).
3. **코칭의 "CS 20개 더 먹었다면 53%"** — 인과 주장이 아니다. 관찰 데이터에서
   **그런 상태였던 팀들의 승률**로 계산한 가정 계산이다. "당신이 CS를 늘리면 승률이
   오른다"를 증명하려면 개입 실험(같은 사람이 행동만 바꿔보는 것)이 필요한데, 우리에겐
   관찰 기록 9,879판뿐이다. 그래서 서비스 문구도 "그랬던 경기들은 이랬다" 수준으로 말한다.

**왜 이렇게까지 조심하나** — 관계의 세계 안에서도 이미 데었기 때문이다.
킬은 효과크기 1.09로 승패와 분명히 관련 있는데 계수는 음수였다(10번).
**관계조차 읽는 방법에 따라 뒤집히는데, 인과는 한 단계 더 위의 주장이다.**
학습 프로젝트가 여기서 선을 넘으면, 맞힌 숫자까지 의심받는다.

## 18. CRISP-DM — 분석 프로젝트의 표준 순서

**뭔가** — *Cross-Industry Standard Process for Data Mining*. 데이터 분석을 어떤 순서로
할지 정리해 둔 절차다. 1990년대에 만들어져 지금도 가장 널리 쓰인다.
업종을 안 가린다(cross-industry)는 뜻에서 이름이 그렇다.

**여섯 단계**

| | 단계 | 하는 일 |
|---|---|---|
| 1 | 업무 이해 | 무엇을 풀 문제인지 정한다 |
| 2 | 데이터 이해 | 어떤 데이터가 있고 상태가 어떤지 본다 |
| 3 | 데이터 준비 | 정리·변환해 모델에 넣을 수 있게 만든다 |
| 4 | 모델링 | 모델을 고르고 학습한다 |
| 5 | 평가 | 목표를 만족하는지 채점한다 |
| 6 | 배포 | 실제로 쓸 수 있게 내보낸다 |

**핵심은 "모델부터 만들지 말라"** 는 것이다. 초보자가 가장 흔히 하는 실수가
데이터를 보기 전에 모델부터 돌리는 것인데, 그러면 데이터에 있는 문제(중복·누수·치우침)를
모델 탓으로 오해하고 엉뚱한 데서 시간을 쓴다.

**한 방향이 아니다.** 평가하다 문제를 발견하면 데이터 준비로 돌아간다.
우리도 그랬다 — 계수 그림에서 이상한 것을 보고 피처로 돌아간 것이 페이즈 2다.

**우리가 바꾼 것 — 분할을 별도 단계로**
표준에서 분할은 "데이터 준비" 안에 묻혀 있다. 그런데 분할은 잘못돼도 **오류가 안 나고**,
이후 점수가 전부 거짓이 되는데 아무도 모른다. 그래서 따로 세우고 네 가지를 확인했다
(겹침·빠진 행·층화·같은 경기 재등장 → 3번 분할과 누수).

## 예상 질문 리허설

| 질문 | 답의 뼈대 |
|---|---|
| 왜 로지스틱인가, 딥러닝은? | 8종 비교 1위 + 과적합 없음 + 해석 가능. 정보 상한이 피처에 있어서 복잡한 모델이 이길 여지가 없었다 (6번) |
| 74%면 낮은 것 아닌가? | 찍기 50% 대비 +24%p. 선행 사례도 70~75%. 남은 오차는 접전 구간의 정보 부족 — 모델이 아니라 데이터의 한계 (2·8번) |
| 나이브베이즈는 왜 탈락? | 독립 가정이 상관 0.9 데이터에서 깨져 확률이 극단으로 쏠림 — Brier 0.2179, 극단 확신 63.6% (15번) |
| 킬이 음수라는 게 말이 되나? | 다중공선성. 단독 효과크기 1.09 vs 통제 후 계수 −0.11은 모순이 아니다. **골드를 빼면 +0.508로 뒤집힌다**(페이즈 2에서 확인) (10번) |
| 시험셋을 정말 안 봤나? | 모델·피처 선택은 전부 훈련셋 교차검증. 시험셋은 최종 평가·해석에만 (3번) |
| 그래서 CS를 먹으면 이긴다는 건가요? | 아니다 — 관찰 데이터의 동반 변화로 계산한 가정 계산이지 개입 효과가 아니다. 인과로 말하는 건 게임 규칙이 보증하는 부분(킬→골드)뿐 (17번) |
| 어떤 절차로 진행했나? | CRISP-DM 을 참고하되 분할을 별도 단계로 뺐다 — 잘못돼도 오류가 안 나는 유일한 단계라서 (18번) |
| 접전은 2단계 모델로 보완하면 되지 않나? | 해봤다 — 4종 전부 단일 모델 이하, 5회 반복 4회 손해. 애매한 경기는 정보가 없는 경기라 구조로는 안 뚫린다 (16번) |
| k=4는 어떻게 정했나? | 실루엣이 결정을 못 해서 해석 가능성으로 골랐다 — 한계로 명시 (13번) |
| 이 결과 믿을 수 있나? | 5-fold + 시드 10회 반복 + 두 해석 방법 교차 + 복원 assert + 문서 정합성 자동 점검 (7번) |
